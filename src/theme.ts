import { createTheme } from "@mui/material/styles";

const theme = createTheme({
    palette: {
        primary: {
            main: "#2d79be",
        },
        mode: "dark",
        background: {
            paper: "#040710",
        },
    },
});

export default theme;
