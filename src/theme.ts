import { createTheme } from "@mui/material/styles";

const theme = createTheme({
    palette: {
        primary: {
            main: "#2d79be",
        },
        mode: "dark",
        background: {
            paper: "#171618",
        },
    },
});

export default theme;
